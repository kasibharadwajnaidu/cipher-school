Lecture34- Insertion Questions
class GFG
{
int getNthFromLast(Node head, int n)
{
Node slow = head;
Node fast = head;
for(int i=0;i<n;i++){
if(fast == null) return -1;
fast = fast.next;
}
while(fast!=null)
{
slow = sloq.next;
fast = fast.next;
}
retrun slow.data;
}
}
